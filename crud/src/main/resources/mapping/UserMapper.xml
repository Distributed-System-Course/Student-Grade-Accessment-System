<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.crud.mapper.usermapper">

    <!--查询所有用户-->
    <select id="userList" resultType="User">
        select * from data
    </select>

    <!--登录验证-->
<!--    <select id="userlogin" parameterType="User" resultType="User">-->
<!--    select * from data where name=#{name} and password=#{password}-->
<!--  </select>-->

    <!--添加用户-->
<!--    <insert id="adduser" parameterType="User" keyProperty="id" >-->
<!--        INSERT INTO user(username,password,age) VALUES (#{username},#{password},#{age})-->
<!--    </insert>-->
    <!-- 众筹项目申请   -->
<!--    <insert id="addproject" parameterType="Product" keyProperty="id" >-->
<!--        INSERT INTO product(pname,intr,tprice) VALUES (#{pname},#{intr},#{tprice})-->
<!--    </insert>-->

    <!--删除用户-->
<!--    <delete id="delete" parameterType="int">-->
<!--        delete from user where id = #{id}-->
<!--    </delete>-->

    <!--根据id查询用户-->
<!--    <select id="findUserById" resultType="User">-->
<!--        select * from user where id = #{id}-->
<!--    </select>-->
    <!--根据id查询用户-->
    <select id="findUserByUserName" resultType="User">
        select * from data where name = #{name}
    </select>

    <!--更改用户信息-->
<!--    <update id="update"  parameterType="user">-->
<!--        update user set username=#{username},password=#{password} where id=#{id}-->
<!--    </update>-->

</mapper>